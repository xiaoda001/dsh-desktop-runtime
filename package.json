{
  "name": "dsh-desktop-runtime",
  "version": "1.0.0",
  "private": true,
  "description": "Build and publish the standalone @deepseek-ai/dsh runtime for dsh-desktop",
  "scripts": {
    "runtime:check": "node scripts/check-latest.mjs",
    "runtime:build": "node scripts/package-runtime.mjs"
  },
  "dependencies": {
    "@deepseek-ai/dsh": "0.1.5-rc.1"
  }
}
